{
  "name": "action-branch-rules",
  "version": "1.0.0",
  "description": "Enforce branch names by validating branch names against valid regexes, and deleting invalid branch",
  "main": "index.js",
  "scripts": {
   "start": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
   "build": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts build"
   "build": "ncc build src/main.js -o dist -m",
   "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/L20234/branch-name-rules.git"
  },
  "author": "AzadehGhaffari",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/L20234/branch-name-rules.git"
  },
  "homepage": "https://github.com/L20234/branch-name-rules.git#readme",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^5.0.0",
    "@vercel/ncc": "^0.28.6"
  }
}
